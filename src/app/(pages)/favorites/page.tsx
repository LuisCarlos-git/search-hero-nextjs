import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Strike team - favorites',
  description: 'List your hero team'
};

export default function Favorites() {
  return <h1>hello favorites</h1>;
}
